import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#6935d3',
    },
    background: {
      default: '#f9f9f9',
    },
  },
});

export default theme;
